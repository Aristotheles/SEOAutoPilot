{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The desktop shell only displays the local SEOAutoPilot service.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

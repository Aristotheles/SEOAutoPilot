{
  "name": "seo-autopilot",
  "version": "0.1.0",
  "private": true,
  "description": "Human-approved Search Console opportunity engine for LingoDecoder.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test test/*.test.js",
    "analyze": "node bin/analyze-export.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
